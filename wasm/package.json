{
  "name": "buttplug-wasm",
  "version": "2.0.1",
  "scripts": {
    "build:main": "tsc -p tsconfig.json",
    "build:web": "vite build",
    "build:wasm": "cd rust; wasm-pack build",
    "build": "yarn build:wasm && yarn build:main && yarn build:web"
  },
  "main": "./dist/buttplug-wasm.mjs",
  "type": "module",
  "dependencies": {
    "//": {
      "buttplug": "^3.2.1"
    },
    "buttplug": "../js",
    "env": "^0.0.2",
    "eventemitter3": "^5.0.1",
    "rust": "./rust"
  },
  "devDependencies": {
    "rollup": "^4.12.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vite-plugin-dts": "^3.7.3",
    "vite-plugin-top-level-await": "^1.4.1",
    "vite-plugin-wasm": "^3.3.0"
  }
}
